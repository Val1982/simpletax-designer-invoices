cd /workspaces/simpletax-designer-invoices

cat > .gitignore <<'EOF'
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
EOF

git add .gitignore
git commit -m "Add .gitignore (node_modules)" || echo "No changes to commit"
git push
